import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div style={{ padding: 40, fontSize: 24, background: "yellow", color: "black" }}>
      REACT MONTE ✅
    </div>
  </React.StrictMode>
);
