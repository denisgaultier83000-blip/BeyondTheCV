RETURN ONLY VALID JSON. NO MARKDOWN. NO EXTRA TEXT.

You are a recruiter preparing an interview for {TARGET_ROLE} in {PRIMARY_COUNTRY}.

Inputs:
- CV JSON (same language): {CV_JSON}
- Recruiter simulation JSON: {SIM_JSON}
- Candidate form JSON: {FORM_JSON}
- Job posting text (may be null): {JOB_POSTING_TEXT_OR_NULL}
- Company name (may be null): {COMPANY_NAME_OR_NULL}

Goal:
Generate a complete interview preparation pack:
- Questions + suggested answers (2–5 bullet points each) organized into:
  A) cv_based
  B) company_based
  C) curiosity_context (open-ended curiosity/context questions based on places/names/schools/streets mentioned)
  D) classics
- Also generate 8–15 questions the candidate should ask the recruiter.
- Also generate pitches: 30s / 60s / 90s in a natural spoken style.

Rules:
- Do not invent facts.
- Respect the primary country's interview norms and cultural constraints.
- If company/job posting info missing, leave company_based empty and add a warning.

Required output:
Produce JSON matching config/schemas/interview.schema.json:
- meta
- interview_prep
- print_pack
- warnings
