name: Backend CI/CD

on:
  push:
    branches: [ master ]
    paths:
      - 'backend/**'
      # Déclenche aussi sur les changements du workflow lui-même
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches: [ master ]
    paths:
      - 'backend/**'

env:
  # Désactive l'avertissement de dépréciation Node.js en forçant la v24
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    name: Run Pytest
    runs-on: ubuntu-latest
    services:
      # Ajoute un conteneur de service PostgreSQL pour les tests
      postgres:
        # [EXPERT FIX] On aligne la version sur la prod
        image: postgres:15-alpine
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpassword
          POSTGRES_DB: testdb
        ports:
          - 5432:5432
        # Option pour attendre que PostgreSQL soit prêt
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # psycopg2-binary est plus fiable en CI que psycopg2
        pip install psycopg2-binary
        pip install -r backend/requirements.txt
        # Ajout de pytest-asyncio pour supporter les tests asynchrones
        pip install pytest pytest-mock pytest-asyncio

    - name: Run Tests
      env:
        # [FIX] Ajoute la racine du projet (.) ET le dossier backend pour corriger tous les imports
        PYTHONPATH: ".:backend"
        
        # [EXPERT FIX] Correction de l'espace, hôte sur 127.0.0.1, et credentials en dur (CI uniquement)
        DATABASE_URL: "postgresql://testuser:testpassword@127.0.0.1:5432/testdb"
        OPENAI_API_KEY: "dummy-key-for-tests"
        GEMINI_API_KEY: "dummy-key-for-tests"
        JWT_SECRET: "dummy-jwt-secret-for-tests"
      run: |
        # Affiche le contenu du répertoire courant pour faciliter le débogage dans les logs
        ls -la
        # Laisse Pytest découvrir automatiquement tous les dossiers et fichiers de tests
        pytest -v -W ignore::DeprecationWarning