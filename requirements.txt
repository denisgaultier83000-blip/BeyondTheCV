# --- Core Framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9  # Requis pour l'upload de fichiers (UploadFile) et les formulaires OAuth2

# --- Database ---
asyncpg>=0.29.0          # PostgreSQL Adapter (Async - FastAPI)
psycopg2-binary>=2.9.9   # PostgreSQL Adapter (Prod/Migration)

# --- AI & LLM Clients ---
openai>=1.12.0           # Client OpenAI (Async)
google-genai>=0.3.0      # Nouveau SDK Google Gemini

# --- Document Processing ---
python-docx>=1.1.0       # Génération et lecture de fichiers Word
pypdf>=4.0.1             # Parsing de fichiers PDF
jinja2>=3.1.3            # Moteur de template (utilisé pour générer le LaTeX)

# --- Security & Authentication ---
python-jose[cryptography]>=3.3.0  # Gestion des tokens JWT
passlib[bcrypt]>=1.7.4            # Hachage des mots de passe
email-validator>=2.1.0            # Validation des emails dans Pydantic

# --- Utilities & External Services ---
python-dotenv>=1.0.1     # Chargement des variables .env
requests>=2.31.0         # Appels HTTP synchrones (Search API)
stripe>=8.1.0            # Gestion des paiements

# Note: pdflatex doit être installé au niveau système (MiKTeX ou TeX Live), pas via pip.